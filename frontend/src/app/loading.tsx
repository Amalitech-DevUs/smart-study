import { Loader } from "@/components/shared/loader";

export default function Loading() {
  return <Loader size="fullscreen" text="Loading SmartStudy..." subtext="Getting your revision materials ready" />;
}
